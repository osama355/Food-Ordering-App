import React from "react";
import { HomeContaier } from ".";

function MainContainer() {
  return (
   <div className='w-full h-auto flex flex-col items-center justify-center'>
    <HomeContaier/>
   </div>
  );
}

export default MainContainer;
